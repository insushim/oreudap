# 오르답 — 정답을 밟고 오르는 무한 학습 계단

구구단과 영단어를 **한눈에 즉답**하며 층을 올리는 세로형 아케이드 학습 게임.
위로 갈라지는 발판마다 답이 적혀 있고, **맞는 답을 밟아야만 한 층 오른다.**

▶ **플레이: https://muhan-pi.vercel.app**

## 설계의 중심 한 줄
학습을 «진행의 1차 자원»으로 둔다. 시간은 벽이고 정답이 열쇠다.
그래서 실력이 곧 기록이 된다 — 실측(게임 코어를 직접 돌린 봇 격자):

| 아는 확률 | 0(순수 찍기) | 0.6 | 0.9 | 1.0 |
|---|---|---|---|---|
| 중앙값 층 | 3 | 10 | 86 | 200 |

찍기·연타·정답 위치 카운팅으로는 오를 수 없다(게이트가 매번 확인한다).

## 아이를 위한 원칙
- **개인정보를 아예 받지 않는다.** 이름·나이·학교·계정·채팅 없음, 입력 필드 0개,
  광고·분석 SDK 0개, 외부 도메인 요청 0건. 기록은 이 기기 localStorage 에만 남는다.
- 오답은 벌이 아니다. 틀리면 **정답 발판을 알려 주고** 오답노트에 담아 며칠 뒤 다시 낸다.
- 코인으로 파는 것은 캐릭터·하늘 같은 **코스메틱뿐**. 돈으로 학습을 건너뛸 수 없다.

## 만든 방식
- **엔진**: Phaser 3.90 (월드만 캔버스) + **DOM 오버레이**(글자·터치 타깃)
  → 44px 터치·28px 글자·대비 4.5:1 을 «실제로 잴 수 있다». 캔버스 오브젝트로는 증명이 불가능하다.
- **코어**(`src/core/`)는 DOM·Phaser 를 import 하지 않는다 → 브라우저 없이 완료 기준을 1:1 테스트.
- **에셋 전부 자체 제작**: 이미지 15장(Meta 생성 후 후처리), 효과음 7종(오프라인 렌더),
  BGM 1곡(ACE-Step 로컬, 이음새 없는 루프). 외부 폰트·CDN 0개. 원장 = `public/assets/asset-license.json`.

## 개발
```bash
npm install
npm run dev        # 개발 서버
npm run gate       # 전체 게이트 14종 (배포 전 이것만 돌리면 된다)
npm run assets     # 에셋 재생성 + 라이선스 원장
npm run docs:sync  # GDD 수치표를 게임 코드에서 재생성
```

## 게이트 (전부 «빨간불이 나오는지» 확인한 것들)
| 게이트 | 무엇을 막는가 |
|---|---|
| 유닛 52건 | 완료 기준 D2~D27 1:1 |
| 밸런스 격자 | 찍기·연타·위치 카운팅 우회, 학습→진행 단조성 |
| 정답 위치 균형 | 정답이 한 자리에 몰리는 것(χ² 과목·갈래별) |
| 학습 데이터 | 곱셈 전수 검산, 낱말 중복·품사·출처 원장 대조 |
| 2D 스모크 | 텍스처 누락(분홍 사각형)·무음·씬 누수·결정론·숨은 탭 |
| 시각 QA | 상태 15장 전수, 44px·28px·대비, 히트테스트, 5개 화면 크기 |
| 플레이 QA | 진짜 키·포인터 입력, 키 홀드, 타임아웃 이후 입력, 개인정보 전수 |
| 성능·번들 | 최악 장면 P95, gzip 초기 전송량, GL 드로우콜 직접 계측 |

## 문서
- `docs/GDD.md` — 기획서(룰·수치·완료 기준 D1~D27). 수치표는 코드에서 재생성된다.
- `docs/RESEARCH.md` · `docs/LEGAL-COMPARE.md` — 장르 조사와 저작권·부정경쟁 비교 검토
- `docs/PRIVACY.md` · `docs/STORE-COMPLIANCE.md` — 개인정보·스토어 아동 정책
- `docs/data-sources.md` — 학습 데이터 출처 원장
- `docs/GDD-XVAL.md` — 기획서 8way 교차검증 기록

## 남은 사용자 확인 항목
1. 게임 이름 확정 후 KIPRIS 상표 검색(제41류·제9류)
2. 교육부 고시 초등 권장 어휘표와 낱말 대조
3. 앱 배포 시 스토어 아동 정책 제출(`docs/STORE-COMPLIANCE.md`)과 변호사·변리사 검토 1회
4. 참조 기기 실측 P95(현재 수치는 headless 구조 경고선이지 실기기 성능이 아니다)

## 일일 등수

첫 화면 아래에 «오늘의 등수»가 있다. 이름은 자동 생성되지만 **아이가 직접 지을 수 있고**,
직접 지은 이름은 **보내기 전에** 가려진다(`김철수` → `김*수`). 서버는 별표 없는 직접 이름을 거부한다.

- API: `worker/` (Cloudflare Worker + KV) — `cd worker && npx wrangler deploy`
- 저장되는 것: 오늘 날짜 · 가려진 이름 · 층수 · 과목, 넷뿐이다. 계정·쿠키·기기 식별자 없음.
- 규칙과 한계 전문 = `docs/PRIVACY.md` (조작 가능성·KV 일관성까지 적어 두었다)
- 🔴 **게이트 봇의 기록은 판에 안 뜬다** — 자동화 브라우저에서 나간 제출은 `t:1` 이 붙어 서버의
  «시험 칸»으로 간다(D36). 게이트를 돌릴 때마다 아이들 판에 가짜 기록이 쌓이던 것을 2026-09-05 에 고쳤다.
- ⚠️ 워커를 고쳤으면 **배포해야 반영된다**(`cd worker && npx wrangler deploy`).
  Cloudflare 계정이 둘이라 **반드시 gmail 계정**이어야 한다 — 편의 스크립트 = `tools/finish-rank-ops.sh`
