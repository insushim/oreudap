# 개인정보 처리 — 오르답

한 줄 요약: **아무것도 수집하지 않습니다.** 수집하지 않은 정보는 유출될 수 없습니다.

## 받지 않는 것
실명·별명 입력 · 나이·생년월일 · 학교·학년·반 · 이메일·전화번호 · 사진·음성 ·
자유 텍스트(채팅·댓글) · 계정·회원가입·로그인 · 광고 SDK · 분석 SDK · 위치 · 연락처.
게임 화면에 **입력 필드가 하나도 없습니다**(게이트 D21 이 0개를 단언합니다).

## 기기에 저장하는 것 (브라우저 localStorage, 키 `oreudap:progress`)
코인 수 · 해금한 캐릭터·하늘 · 과목별 최고 층 · 오답노트 박스 진행도 ·
오늘의 미션 진행 · 소리·흔들림·색약 설정.
이 값들은 **이 기기의 브라우저 안에만** 있습니다. 서버로 보내지 않고, 저희도 볼 수 없습니다.
브라우저의 「사이트 데이터 삭제」로 언제든 전부 지울 수 있습니다.

## 네트워크
게임에 필요한 파일(HTML·자바스크립트·그림·소리)을 **같은 도메인에서 GET 으로만** 받습니다.
외부 폰트·CDN·분석 서버로 나가는 요청이 0건입니다 — 아이의 IP 주소가 제3자에게 가지 않도록
글꼴은 기기에 이미 있는 시스템 글꼴만 쓰고, 라이브러리는 자체 호스팅합니다.
게이트 D21 이 배포 전마다 전수 확인합니다(외부 요청 0 · POST 0 · 쿼리스트링 0 · 요청 본문 0).

## 서버 로그
정적 호스팅이라 접속 로그는 호스팅 제공자의 기본 웹서버 로그(IP·시각·요청 경로)뿐이며,
저희가 별도로 수집·보관·분석하지 않습니다.

## 아이를 위한 설계 원칙
- 랭킹을 넣더라도 자유 입력 이름은 쓰지 않습니다(생성 닉네임만).
- 오답이 «벌»이 되지 않게 만듭니다. 못하는 아이가 더 나빠지는 장치를 두지 않습니다.
- 학습 기록은 기기 안에서 보여 주기만 합니다. 이메일 발송·서버 전송이 없습니다.

## 문의
이 문서와 실제 동작이 다르다고 느끼면 알려 주세요. 게이트 로그(`qa/`)로 확인해 드립니다.

## 일일 등수 (2026-09-04 추가)

이 기능이 들어오면서 「외부 요청 0건 · 입력 칸 0개」 설계가 바뀌었다. **무엇이 바뀌고
무엇이 안 바뀌었는지**를 분명히 적어 둔다.

### 바뀐 것
- 바깥으로 나가는 요청이 **하나** 생겼다: `https://oreudap-rank.simssijjang-d79.workers.dev/api/rank`
- 아이가 이름을 지을 수 있는 **입력 칸 하나**가 생겼다(`#nick`, 최대 8자)

### 안 바뀐 것 — 그리고 오히려 강해진 것
- 계정·회원가입·비밀번호 **없음**. 나이·생년월일·학교·반·이메일·전화번호 **안 받음**
- 광고 SDK·분석 SDK **없음**. 쿠키 **없음**. 기기 식별자 **없음**
- 서버에 저장되는 것은 **오늘 날짜 · 가려진 이름 · 층수 · 과목** 넷뿐이다
- 서버에는 «어제의 나»와 «오늘의 나»를 잇는 길이 **없다**. 성장 기록은 기기 안에만 산다

### 이름을 다루는 규칙
1. 기본값은 **자동 생성**(`빠른여우07` 같은 3만 가지 조합). 아무것도 안 해도 등수판에 오른다.
2. 아이가 직접 지을 수 있다. 다만 **학년·반 꼬리표**(`5학년3반김철수`), 욕설, 한글 낱자,
   특수문자, 8자 초과는 받지 않는다.
3. 🔴 **가리는 자리는 «보여줄 때»가 아니라 «보내기 전»이다.** 직접 지은 이름의 원문은
   이 기기를 영영 벗어나지 않는다 — `김철수` 는 `김*수` 가 된 뒤에야 네트워크로 나간다.
4. 서버는 별표 없는 직접 이름을 **거부한다**(`worker/src/rank-core.js` 의 `acceptName`).
   즉 3번은 예의가 아니라 게이트다. 자동 생성 이름은 사람을 가리키지 않으므로 가리지 않는다.

### 이 성질을 기계가 잰다 (D21)
`tools/qa-play.mjs` 가 이름 칸에 `김철수` 를 넣고 한 판을 정산시킨 뒤, **나간 모든 바이트**에서
그 글자를 찾는다. 한 건이라도 나오면 실패한다. 제출 본문의 필드가 `{n, s, sub}` 셋인지,
보낸 이름에 별표가 있는지도 함께 본다. 예전의 「외부 요청 0건」은 지키려던 성질의 *대리 지표*였고,
지금은 그 성질 자체를 직접 잰다.

### 남아 있는 한계 (숨기지 않는다)
- 오르답은 싱글 플레이라 **점수를 클라이언트가 보낸다.** 서버 권위 시뮬레이션이 없으므로
  마음먹으면 조작할 수 있다. 층수 상한(500)과 이름당 한 줄이 방어의 전부다.
  → 이 판은 「친구들과 보는 오늘의 기록판」이지 공식 대회 기록이 아니다.
- 판 저장소는 KV(최종적 일관성)라, 같은 순간에 둘이 제출하면 한쪽이 덮일 수 있다.
- 아이가 자기 실명을 «그대로» 지어 넣으면 `김*수` 로 가려지지만, 같은 교실에서는
  누구인지 짐작될 수 있다. 이건 기술로 못 막는다 — **교사가 등수판을 볼 수 있다는 점이
  최종 방어**다(리로드 아레나와 같은 판단).
