# GDD — 「오르답」 (가제) : 정답을 밟고 오르는 무한 학습 계단

작성 2026-09-04 · game-builder v7.1 Phase 1 · 상태: **승인 대기(코드 0줄)**
수치 정본 = `tools/probe-grid.mjs`(게임 코어를 직접 import 하는 프로브) 출력. §1-4-2 표는 `tools/sync-gdd-table.mjs` 가 생성하고 `--check` 가 어긋남을 막는다(손계산 전사 금지). 법무 근거 = `docs/RESEARCH.md` §3.

## 1-1. 게임 개요
- **이름(가제)**: 오르답 — "오르다 + 답". 대안: 「답계단」「스텝답」. 배포 전 KIPRIS 상표 검색 필수(사용자 항목).
- **장르**: 즉답 학습 × 엔드리스 계단 오르기 아케이드 (2D, 세로 화면)
- **플랫폼**: 웹(모바일 브라우저 1순위, 데스크톱 겸용) → PWA → Capacitor(iOS/Android) 순
- **타겟**: 초등 2~6학년(구구단 = 2학년, 영단어 = 3~6학년), 부모·교사 보조 사용
- **한 줄 컨셉**: 위로 갈라지는 발판마다 답이 적혀 있다. **맞는 답을 밟아야만 한 층 오른다.** 틀리면 발판에 금이 가고, 시간이 다하면 발판이 사라진다.
- **세계관**: 구름 위 «지식의 탑». 층마다 다른 하늘(새벽→낮→노을→별밤)로 오르며 스킨 캐릭터를 수집한다. 스토리 텍스트 최소(아이가 읽지 않고 바로 시작).
- **상업 모델(v1)**: 웹 무료 + 앱스토어 **일회성 유료(₩3,900 안)** 또는 학교·학원 단체 라이선스. **광고 SDK·인앱 결제·계정 없음**(아동 개인정보 0 원칙). 결제 도입은 v2 중대 변경으로 별도 FULL 검증.

## 1-2. 코어 루프
```
[문제 카드 표시] → [갈래 발판 중 정답 탭] → [한 층 상승·코인] → [층↑: 타이머 단축·3갈래 등장] → 반복
      ↑                                 ↓ 오답/시간초과 → 하트 −1, 정답 발판 하이라이트(교정)
      └──── 하트 0 → 게임오버 → 결과(층·정답률·오답노트) → «한 판 더»(2탭 이내)
```
- **학습 = 진행의 1차 자원**(L-041): 층을 올리는 유일한 방법은 정답. 시간은 «벽», 정답은 «열쇠».
- **메타 루프**: 코인 → 스킨·탑 테마 해금(코스메틱만) / 마스터리 맵(구구단 단별 ★, 단어 박스 진행도 `●●○○`) / 일일 미션 3개 / 최고 층 기록(로컬).
- **세션 길이**: 한 런 60~180초(정답률 90% 봇 기준 약 98문항·중앙값 80층). **앱 첫 화면 기준** 첫 문제까지 총 **3탭 이내**(과목 카드 1 + 범위 확인 1 + 시작 1 — D1 과 같은 기준점).
- **한 판 더 장치**: 게임오버 시 «최고 기록까지 N층» 표시, 재시작 버튼 2초 내 활성, 오답 1개를 카드로 보여주고 «이번엔 이거 맞히기» 미션 자동 부여.

## 1-3. 핵심 메카닉 상세
### 1-3-1. 조작
| 입력 | 데스크톱 | 모바일 |
|---|---|---|
| 왼쪽 갈래 | ← 또는 A | 화면 왼쪽 1/3 탭 |
| 가운데 갈래(3갈래 시) | ↑ 또는 W | 화면 가운데 1/3 탭 |
| 오른쪽 갈래 | → 또는 D | 화면 오른쪽 1/3 탭 |
| 일시정지 | Esc | 상단 우측 버튼(44px+) |
- 입력은 «발판이 확정된 뒤»부터만 받는다. 활성화 이전·해결 국면·타임아웃 이후의 입력은 **버퍼링 없이 무시**한다 — 선택지는 활성화 순간에 처음 보이므로 그 전의 탭은 «보지 않은 문제에 대한 답»이 된다(2026-09-04 교차검증 3계열 합의). 키 자동 반복(`e.repeat`)도 무시한다. 문항당 판정 **정확히 1회**.

### 1-3-2. 문항·판정
- 문제 카드가 상단에 뜨고, 같은 순간 갈래 발판 위에 선택지가 찍힌다. 정답률 판정은 **탭한 갈래 = 정답 갈래** 뿐이다(L-042: 우연 접촉이라는 개념 자체가 없다).
- **정답**: 캐릭터가 그 발판으로 점프(트윈 220ms, 착지 스쿼시), 층 +1, 코인 +1, 스트릭 +1(스트릭이 10의 배수가 되면 코인 +5·하트 +1), 정답 SFX.
- **오답**: 밟은 발판에 금이 가며 캐릭터 헛디딤(280ms), 하트 −1, 스트릭 0, **정답 발판 0.8초 하이라이트 + 정답 읽어주기 텍스트**(교정 피드백). **하트가 남아 있으면** 정답 발판으로 자동 이동해 다음 문항 진행(층은 오르지 않음). **하트가 0이면** 하이라이트 후 자동 이동·다음 문항 없이 결과 화면으로 간다.
- **시간초과** = 오답과 동일 처리(발판이 흐려지며 사라지는 연출).
- **하트**: 시작 3, 최대 3, **스트릭 10마다 +1**. 0 → 게임오버. 🚫 코인/광고 부활 없음(교육 게임 부활 금지 룰). 부활 대신 «오답노트 3문제 연속 정답 → 1회 부활»은 v2 검토 항목.

### 1-3-3. 갈래 수 (난이도 축 1)
| 층 | 갈래 |
|---|---|
| 1~29 | 2갈래(좌·우) |
| 30~59 | 2 또는 3갈래(50%) |
| 60+ | 3갈래(좌·중·우) |

### 1-3-4. 정답 위치 — 위치 덱 (HARD, L-109)
정답 위치는 셔플이 아니라 **덱**으로 고른다. 갈래 수별로 **독립 덱** 운용(30~59층 혼합 구간에서 2갈래 문항은 2갈래 덱만, 3갈래 문항은 3갈래 덱만 뽑는다): 2갈래 덱 `[L,L,L,R,R,R]`, 3갈래 덱 `[L,L,C,C,R,R]`.
- **카운팅 방어**: 덱을 끝까지 소진하지 않는다 — 잔여 2장 이하가 되면 새 덱을 섞어 **잔여분과 합쳐** 계속 뽑는다(마지막 위치가 이력만으로 확정되는 순간을 없앤다).
- **병합 불변식**: 병합 직후 «직전 출제 위치 이력 + 새 덱 순서»에서 같은 위치 4연속이 생기면 재셔플(최대 20회, 그래도 생기면 4번째 카드를 다른 위치와 교환). D8 표본은 병합 경계를 반드시 포함한다(덱 길이의 10배 이상 연속 출제).
- 게이트 ① `check-answer-balance.mjs` **과목별·갈래 수별** 실행, 종료코드 0. ② **위치 이력 공격 봇**(문제를 안 보고 지난 위치 이력만으로 가장 유력한 위치를 고르는 p=0 봇)의 중앙값 층이 균등 p=0 봇 중앙값 +1 이하(D8b).

### 1-3-5. 오답(distractor) 생성 규칙
- **구구단**: 정답 a×b 에 대해 후보 = {a×(b±1), (a±1)×b, **(a±1)×(b∓1)**} ∪ **흔한 오류 사전**(`gugudan-common-errors.json`, 예: 7×8→54, 6×7→48, 8×7→54, 9×6→56) 중 정답과 다른 값. 우선순위 = 흔한 오류 사전 > 교차 후보 > 인접 후보. 2갈래면 1개, 3갈래면 2개. 오답끼리도 서로 달라야 함. 0·음수·같은 값 제외. D9 는 이 합집합을 기준으로 전수 검사한다.
- **영단어**: 같은 학년·같은 품사 뜻에서 추출, 정답 뜻과 첫 글자 다른 것 우선(한눈에 구분되되 «생김새»로 못 맞히게). 단어→뜻, 뜻→단어 두 방향을 50:50.
- 문항 반복 규칙: **무작위 출제**에서는 직전 5문항과 같은 문항 금지. **복습 큐 재출제(§1-3-6)는 이 규칙의 예외**이며 우선한다(3문항 뒤 재출제는 의도된 반복).

### 1-3-6. 적응형 출제 (SRS-lite)
- 세션 복습 큐: 틀린 문항을 `[3, 7, 15]` 문항 뒤에 재출제, 3회 정답 시 큐 졸업(이번 판만).
- 영속 오답노트: `BOX_DAYS=[1,3,7,16]`, `MAX_BOX=4`, localStorage. 약점 항목 출제 가중 **최대 3배**(더 자주, 더 어렵게 X).
- 화면 문구 분리: «👍 오늘은 통과!»(세션) ≠ «🎓 완전히 내 거!»(영속 박스 4). 배지는 영속 박스를 읽는다.

### 1-3-7. 과목·범위 (v1)
| 과목 | 범위 | 문항 수 |
|---|---|---|
| 구구단 | 2~9단 × 1~9 (단 선택 가능, «전체» 포함) | 72 |
| 영단어 3~4학년 | 교육부 초등 권장 어휘 중 3~4학년 160단어 | 160 × 2방향 |
| 영단어 5~6학년 | 같은 범위 5~6학년 160단어 | 160 × 2방향 |
| (v2) 덧셈·뺄셈 1~2학년 / 한자·속담 | — | — |
데이터 파일 = `src/data/{gugudan,words-g34,words-g56}.json`. 뜻·예문·오답 전부 자작(RESEARCH §3 항목 3-4). **데이터 정확성은 릴리스 차단 기준(D23)**: 단어 철자·품사·학년 배정은 출처를 적은 원장 `docs/data-sources.md`(교육부 고시 어휘표 항목 번호)와 대조하고, 곱셈값은 스크립트로 전수 검산하며, 뜻·예문은 Phase 3 에서 FULL 교차검증(날짜·사실 룰) 통과 기록을 남긴다.

## 1-4. 밸런싱 수치 (전부 `tools/probe-grid.mjs` 출력)
### 1-4-1. 상수
| 항목 | 값 |
|---|---|
| 문항 타이머 T(층) | `max(1.0, 3.2 − 0.011×층)` 초 → 층1 3.19s, 층100 2.1s, 층200 1.0s(바닥) |
| 하트 | 시작 3 / 최대 3 / 스트릭 10마다 +1 |
| 3갈래 시작 | 층 30(50%) → 층 60(100%) |
| 활성화 이전 입력 | 무시(버퍼 없음) |
| 점프 트윈 / 헛디딤 | 220ms / 280ms(+정답 하이라이트 800ms) |
| 코인 | 층당 +1, 스트릭 10마다 +5, 일일 미션 +20×3 |
| 스킨 가격 | 60 / 120 / 240 / 480 / 900 / 1,500 코인(6종) · 탑 테마 300 / 600 / 1,200(3종, 기본 1) |

### 1-4-2. 봇 격자 (게임 코어를 직접 import 한 프로브 출력)
<!-- PROBE-TABLE:BEGIN 자동 생성 — tools/sync-gdd-table.mjs. 손으로 고치지 말 것 -->
공통난수 시드 1..300, 반응시간 N(0.85s, 0.20s), 모르면 남은 선택지 균등 추측. 과목=구구단.
p = «아는 확률»이고 실제 정답률은 추측 성공분이 더해진 값이다(같은 행의 «실제 정답률» 칸).

| 아는 확률 p | 중앙값 층 | p10 | p90 | 코인 중앙값 | 실제 정답률 | 시간초과율 |
|---|---|---|---|---|---|---|
| 0 (순수 찍기) | 3 | 0 | 7 | 3 | 42.9% | 0.0% |
| 0.5 | 8 | 2 | 17 | 8 | 67.7% | 0.0% |
| 0.6 | 10 | 4 | 20 | 10 | 73.2% | 0.0% |
| 0.7 | 14 | 7 | 31 | 15 | 79.5% | 0.0% |
| 0.8 | 25 | 9 | 51 | 31 | 84.3% | 0.1% |
| 0.9 | 65 | 20 | 79 | 89 | 90.8% | 1.3% |
| 0.95 | 76 | 60 | 82 | 106 | 93.8% | 2.8% |
| 1 | 79 | 73 | 83 | 113 | 96.1% | 3.9% |
| 1.0 · 반응 N(0.70, 0.15) | 91 | 84 | 116 | 131 | 96.5% | 3.5% |
| 1.0 · 반응 N(1.10, 0.25) | 65 | 59 | 69 | 92 | 95.2% | 4.8% |
<!-- PROBE-TABLE:END -->

- 단조 증가 ✅. 순수 찍기(p=0)로는 못 오른다. 못해도(p=0.5) 게임이 되고, 다 알아도(p=1.0) 타이머 벽에서 끝난다 — 반응이 빠른 봇만 더 간다(표 하단 2행 = 실력 천장).
- 게이트 = `npm run gate:balance`(D10·D8b). 연타봇·위치이력봇이 찍기봇보다 유리하면 FAIL 한다.
- 해석: 층 200 이전엔 «아는가»가, 그 이후엔 «얼마나 빨리 아는가»가 벽. 층 200 벽이 너무 이르면 Phase 4 에서 `TMIN`·`K` 만 조정(구조 불변).

### 1-4-3. 경제
- 90% 플레이어 1런 수입 = §1-4-2 표의 **코인 중앙값 117**(층 + 스트릭 보너스가 합쳐진 분포의 중앙값이며, 오답이 스트릭을 끊는 효과가 이미 들어 있다). 하루 3런(351) + 일일 미션 60 = 411코인 → 6종 스킨 전부(3,300) ≈ 8일, 테마까지(5,400) ≈ 14일. 인플레 방지 = 코스메틱만 판매·가격 상승 계단·재화 흡수처(테마).
- **일일 미션(학습 행위에만 결부, 각 +20코인)**: ① 오늘 정답 30개 ② 스트릭 10 달성 1회 ③ 오답노트 문항 5개 정답. 런 횟수·앱 실행 같은 학습 무관 조건 금지(D27).

## 1-5. UI/UX
- **화면**: 타이틀(과목 카드 3장 + 스킨/기록/설정) → 플레이 → 결과(층·정답률·오답 카드·한 판 더) / 오답노트 / 컬렉션(스킨·테마) / 설정(사운드·진동·색약 모드·reduced motion·한글/영어 UI).
- **HUD(플레이)**: 상단 중앙 **층 카운터**, 좌상 하트 3, 우상 일시정지, 상단 1/4 **문제 카드**(카드 아래 **타이머 바**), 화면 중앙 캐릭터, 캐릭터 위쪽에 갈래 발판(선택지 텍스트 28px+ 볼드), 하단 1/3은 투명 탭 영역(3분할 가이드 점선은 첫 3판만).
- **가독성**: 선택지 텍스트 최소 28px(모바일 360px 기준), 대비 4.5:1 이상(알파 포함), 탭 타깃 44px 이상, 색약 모드는 정답/오답을 색+아이콘(✓/✗)+모양으로 이중 표기.
- **전환**: 층 상승 시 카메라 위로 이징(180ms), 배경 패럴랙스 3레이어, 50층마다 하늘 색 전환(크로스페이드 1s). 결과 화면은 1초 내 등장.
- **연출(타격감 최소 세트)**: 정답 = 착지 스쿼시 + 스파클 파티클 12 + 코인 팝 / 오답 = 발판 크랙 + 화면 셰이크 4px 120ms(reduced motion 시 OFF) + 하트 깨짐 / 스트릭 10 = 캐릭터 뒤 불꽃 트레일.

## 1-6. 에셋 목록 (모션은 생성하지 않고 코드 변형 — 스킬 HARD 룰)
| 종류 | 항목 | 수 | 규격 |
|---|---|---|---|
| 캐릭터 | 기본 1 + 스킨 6 (정지 키아트, 점프·헛디딤은 트윈/스쿼시) | 7 | 256×256 투명(순백+cutout) |
| 발판 | 테마별 발판 normal/correct/crack/fade | 4테마×4 | 320×96 |
| 배경 | 테마 4 × 패럴랙스 3레이어 + 하늘 4단계 | 16 | 1080×1920 |
| UI | 버튼 3상태, 패널, 하트, 코인, 카드 프레임, 과목 아이콘 3, 설정 아이콘 6 | ≈20 | 벡터 스타일 |
| 이펙트 | 스파클, 크랙 파편, 불꽃 트레일, 코인 팝 | 4 | 시트 또는 파티클 텍스처 |
| 타이틀 | 로고, 앱 아이콘 | 2 | 1024² |
| BGM | 강도 3단계 루프(잔잔 → 조임 → 질주) — 층 경계 `balance.TIER_FLOORS`(10·25층)에서 크로스페이드, 단계 안에서는 재생속도 1.00→1.12 램프 | 3 | gen-bgm, 곡당 ≤2MB. 2·3단계는 **지연 로드**(첫 문제 전송량 예산 보호) |
| SFX | 정답, 오답, 시간초과, 점프, 착지, 하트 회복, 스트릭, 코인, 버튼, 게임오버, 신기록 | 11 | gen-sfx, 총 ≤300KB |
| 낱말 발음 | 영단어 1207개 중 943개(78%) — EchoTale 자산 재인코딩(AAC 32k 모노, 평균 4.1KB). 나머지 264개는 브라우저 음성 | 943 | **온디맨드**(번들 예산과 분리, §1-9) |
에셋 12개 초과 → **쇼룸 필수**. 경로는 `assets/manifest.json` 단일 진실원. 라이선스 원장 `asset-license.json` + `free-assets.sh verify` PASS.

## 1-7. 교육 연동
- **교과 연계(2022 개정)**: 수학 2학년 «곱셈구구» / 영어 3~6학년 기본 어휘(듣기·읽기 어휘 인식).
- **학습 시스템**: §1-3-5~6. 즉답 인식(recognition) 훈련이 목적이라 2~3지선다가 타당하며, 회상(recall)은 v2 «빈칸 입력 모드».
- **학습 이력(기기 내)**: 과목별 정답률, 단별/단어별 박스, 취약 TOP5, 최근 7일 런 수. 서버 전송·이메일 발송 없음. 부모/교사 리포트는 화면 캡처/인쇄용 페이지만.
- **개인정보**: 실명·나이·학교·자유 입력·계정·광고/분석 SDK 전부 없음. 폰트·라이브러리 자체 호스팅.

## 1-8. 완료 기준 (Definition of Done) — 각 항목 = Phase 4 테스트 1개
- [ ] D1. **앱 첫 화면(타이틀)부터 세어** 총 3탭 이내에 첫 문제 카드와 갈래 발판이 표시된다.
- [ ] D2. 정답 갈래를 탭하면 층이 정확히 1 오르고 코인이 1 증가하며, 스트릭이 10의 배수가 되는 정답에서만 코인이 추가로 5 증가한다. 오답/시간초과는 층이 오르지 않고 하트가 1 줄며 정답 발판이 800ms 하이라이트된다. 하트 0 이 된 오답 뒤에는 다음 문항이 생성되지 않는다.
- [ ] D3. 문항당 판정은 1회다(연타·동시 탭에도 층/하트 변화 합계가 1을 넘지 않는다).
- [ ] D4. 하트 0 → 결과 화면이 1초 내 뜨고 «한 판 더»가 2탭 이내에 새 런을 시작한다.
- [ ] D5. 스트릭 10마다 하트가 +1(최대 3)된다.
- [ ] D6. 타이머 T(층)이 §1-4-1 식과 일치한다(층 1·100·200 에서 단언).
- [ ] D7. 갈래 수: 층 1~29 는 항상 2, 층 60+ 는 항상 3(확정 단언). 층 30~59 는 시드 고정 N≥400 표본에서 3갈래 비율이 50%±5%p(표본 미달은 «측정 무효» FAIL).
- [ ] D8. **정답 위치 덱**: `check-answer-balance.mjs` 과목별·갈래 수별 rc 0(각 ≥300표본). 같은 위치 4연속 0건.
- [ ] D8b. **위치 이력 공격 봇** 중앙값 층 ≤ 균등 p=0 봇 중앙값 +1(공통난수, 시드 5). 자기검사: 소진형 덱으로 되돌리면 FAIL 해야 한다.
- [ ] D9. 오답 생성 규칙: 구구단 오답은 정답과 다르고 서로 다르며 §1-3-5 합집합(후보식 ∪ 흔한 오류 사전) 안이다(72문항 전수). 영단어 오답은 같은 학년·같은 품사다(전수).
- [ ] D10. **학습→진행 격자 게이트**(공통난수, 시드 5): p=0/0.5/0.6/0.7/0.8/0.9/1.0 중앙값 층이 단조 증가하고 0.6↔0.9 격차 ≥ 30층, p=0 봇 중앙값 ≤ 4층(찍기 우회 불가), 0.5 봇 중앙값 ≥ 5층, 반응 0.85s·p=1.0 봇은 중앙값 ≤ 400층. 자기검사: «정답=진행» 연결을 끊으면 이 게이트가 FAIL 해야 한다.
- [ ] D11. 세션 복습 큐 `[3,7,15]` 가 5문항 반복 금지보다 우선해 정확히 그 간격에 재출제되고, 영속 박스 `[1,3,7,16]일` 이 동작하며 배지가 영속 박스를 읽는다(날짜 모킹). **오답노트에는 틀린 적 있는 문항만 담긴다** — 다 맞힌 판 뒤 노트는 비어 있다.
- [ ] D12. 세이브 키 `oreudap:progress` `{version,data}` 래퍼 + v1→v2 마이그레이션. 손상·조작된 저장값(문자열 "Infinity" 코인·객체로 들어온 배열·깨진 오답노트 항목·모르는 버전)이 예외 없이 정합화되고, 정산은 런당 정확히 1회다.
- [ ] D13. 끝단 입력: 진짜 KeyboardEvent·포인터. t=0 = 갈래 발판 활성화 시각. 기대값 ① t < 0(활성화 전): **무시** ② t ∈ [0, T): 즉시 1회 판정 ③ t ≥ T(타임아웃 이후): **무시**(하트 변화는 타임아웃분 −1 만). 사람 오차 ±90ms 를 실어 키 **n≥20** · 포인터 **n≥20** · 타임아웃 이후 **n≥20**, 기대와 다른 결과 0건. 키 자동 반복은 판정되지 않는다. 표본 미달은 «측정 무효» FAIL.
- [ ] D14. 봇이 한 판을 끝까지 두는 플레이테스트(브라우저 `page.mouse.click`)에서 런타임 오류 0.
- [ ] D15. 상태별 캡처 전수: 타이틀·플레이(2갈래·3갈래)·오답 하이라이트·결과·오답노트·컬렉션·설정·**landscape**·360×640·1080×1920.
- [ ] D16. 탭 타깃 ≥44px (`--mobile --check-tap-targets` 단언) · 선택지 텍스트 ≥28px · 대비 ≥4.5:1(알파 포함).
- [ ] D17. `phaser-smoke.mjs --scene-cycles 5` rc 0(텍스처 키 전수·오디오 디코드 N/N·숨은 탭 전진·씬 누수).
- [ ] D18. 브라우저 콘솔 에러·경고 0건(`--fail-on-console`).
- [ ] D19. 결정론: `check-determinism.sh` 비시드 난수 0건, 문항 시퀀스가 시드로 재현된다.
- [ ] D20. `free-assets.sh verify` PASS, 크레딧 화면 자동 생성, 폰트 OFL 자체 호스팅.
- [ ] D21. 개인정보: 한 판 전체 HAR 에서 **바깥으로 나가는 곳은 등수 API(`/api/rank`) 하나뿐**이고, 자체 도메인 요청은 본문 없는 정적 GET 뿐이다. 요청 본문·헤더에 localStorage 값 0건. 계정·쿠키·기기 식별자·광고/분석 SDK 0. 보존 정책은 `docs/PRIVACY.md`.
  - 🔴 2026-09-04 규칙 변경: 예전 기준은 「외부 요청 0건 · 입력 필드 0개」였다. 일일 등수가 들어오며 그 **대리 지표**는 더 못 쓴다 — 지키려던 성질을 D29 가 **직접** 잰다(실명이 나가는가). 대리 지표를 성질로 바꾼 것이지 기준을 낮춘 것이 아니다.
- [ ] D22. 첫 사용자 입력에서 AudioContext 언락 → 정답 SFX 재생 확인.
- [ ] D23. **학습 데이터 정확성(릴리스 차단)**: 곱셈 72문항 스크립트 검산 0오류, 영단어 320개 철자·품사·학년이 `docs/data-sources.md` 원장과 1:1 대조 0오류, 뜻·예문 FULL 교차검증 통과 기록(`script-xval.json` 형식) 존재.
- [ ] D24. **스토어 아동 정책(앱 배포 시)**: Google Play 가족 정책 설문·데이터 안전 섹션 «수집 없음», Apple Kids 카테고리 요건·개인정보 영양표 «수집 없음», 자체등급분류(GRAC) 전체이용가, 앱 권한 요청 0개 — 체크리스트 `docs/STORE-COMPLIANCE.md` 전 항목 증빙 캡처.
- [ ] D25. **성능·번들 자동 게이트**: 참조 기기(§1-9 SKU)에서 최악 장면 스크립트 실행 로그에 «worst-scene=true» 와 P95 값이 찍히고 합격선 이내(headless 는 **3회 재서 최속 회차**로 판정한다 — 같은 코드가 22~66ms 를 오가서 한 번만 재면 무작위로 빨간불이 켜진다. 시간은 최속, 드로우콜은 최대로 집계한다). 초기 로드 gzip 전송량(HAR transferSize 합) ≤ 3MB, dist 전체 ≤ 8MB, 최악 장면 드로우콜(렌더러 카운터 로그) ≤ 60 — 셋 다 초과 시 rc 1.
- [ ] D26. **법무 비교 검토 문서** `docs/LEGAL-COMPARE.md`(RESEARCH §3-6) 작성·사용자 확인 완료. 앱 배포 전 변호사/변리사 검토 1회(사용자 항목).
- [ ] D27. 일일 미션 3종이 §1-4-3 정의(정답 30 / 스트릭 10 / 오답노트 5 정답)로만 달성되고, 런 반복·앱 실행만으로는 달성되지 않는다.
- [ ] D28. **올라가는 느낌** — 정답 12회를 실제 클릭으로 넣었을 때 ① 캐릭터의 화면 높이가 흔들리지 않는다(움직이는 것은 세계다) ② 캐릭터 아래에 지나온 계단이 2개 이상 보인다 ③ 계단이 한 층의 80% 이상 간격으로 떨어져 있다 ④ 캐릭터 머리와 «올라갈 발판» 사이에 8px 이상 여유가 있다 ⑤ 배경이 층당 한 층 높이의 40% 이상 흐른다 ⑥ 10층 금 발판이 남는다. (`tools/qa-climb.mjs`)
  - 🔴 이 항목이 생긴 이유: 14개 게이트가 전부 초록불인 채로 «점프하고 제자리»인 빌드가 배포됐다. 재지 않는 것은 지켜지지 않는다.
- [ ] D29. **일일 등수에 실명이 실리지 않는다** — 이름 칸에 `김철수` 를 넣고 한 판을 정산시킨 뒤, 나간 모든 바이트에 그 글자가 0건. 제출 본문의 필드는 `{n, s, sub, m, t}` 다섯뿐이고(`m`=모드, 세 값 중 하나인 열거형 / `t`=봇 표시, 아래 D36), 직접 지은 이름에는 별표가 있다. 서버(`acceptName`)도 별표 없는 직접 이름을 거부한다. 입력 칸은 이름 하나뿐이며 maxlength ≤ 8. (`tools/qa-play.mjs` · `tests/rank.test.js`)
- [ ] D36. **게이트가 아이들 등수판을 더럽히지 않는다** — 자동화 브라우저(`navigator.webdriver`)에서 나간 제출에는 `t:1` 이 붙고, 서버는 그 줄을 «시험 칸»(`t-<날짜>`)에 넣어 오늘 판에서 뺀다. 봇이 400층을 올려도 판에는 한 줄도 안 뜬다.
  - 🔴 왜 «안 보내기»가 아닌가: D29 는 **실제로 서버까지 나간 바이트**를 검사해야 뜻이 있다. 안 보내면 D29 가 아무것도 재지 않게 된다. 그래서 보내되 칸을 나눈다 — 쓰기·목록 조회·등수 계산은 그대로 돈다.
  - 🔴 왜 게이트마다 플래그를 심지 않는가: 규칙이 게이트 7개에 흩어지면 반드시 절반만 고쳐진다(같은 날 `seenHow` 를 qa-live 하나에서 빠뜨려 배포 후 터졌다). 자동화 브라우저는 스스로 신분을 밝히므로 클라이언트가 그 한 줄만 읽으면 앞으로 게이트가 몇 개 늘어도 자동으로 걸린다. (`tools/qa-play.mjs` · `tests/rank.test.js`)
- [ ] D31. **올라갈수록 조여드는 BGM** — 정답을 계속 넣어 28층까지 오르는 동안 ① 한 단계 안에서 재생속도가 오른다 ② 10·25층에서 트랙이 `bgm→bgm-tense→bgm-rush` 로 실제로 바뀐다(지연 로드가 제때 도착한다) ③ 경계에서 속도가 1.0 으로 되돌아가고 어디서도 1.12 를 넘지 않는다 ④ 크로스페이드 뒤에도 재생이 끊기지 않는다 ⑤ 첫 문제 전에는 강도 트랙을 한 건도 받지 않는다. (`tools/qa-bgm.mjs`)
- [ ] D32. **영어를 읽어 준다** — 영어→뜻 문제는 낱말을 보여 주면서 파일 발음을 재생하고, 뜻→영어 문제는 **풀기 전에 읽지 않는다**(읽으면 정답을 알려 주는 것이다). 파일이 없는 264개 낱말은 브라우저 음성으로 넘어가 무음이 0이며, 낱말 파일은 첫 문제 전에 한 건도 받지 않는다. (`tools/qa-voice.mjs`)
- [ ] D33. **모드가 서로 다른 이유로 끝난다**(코어 프로브) — p=0.9 봇 300판에서 아슬아슬은 기력 고갈 ≥55%, 60초 질주는 시간 종료 ≥60%, 클래식은 오답 ≥90%. 아슬아슬은 판의 10~65%를 «위험 구간»에서 보낸다(그 밖이면 긴장이 없거나 늘 빨간 화면이다). 모든 모드에서 실력↑ → 층↑ 단조, 반만 아는 봇은 완벽한 봇의 50% 미만. (`tools/probe-modes.mjs`)
- [ ] D34. **모드가 화면에서도 모드다** — 고른 모드로 판이 시작되고, 아슬아슬은 기력 막대가 보이고 «실제로 줄어들며» 하트가 사라진다. 위험 구간에서 연출이 켜지고 기력 0 에서 판이 끝나며 종료 후 연출이 정리된다. 60초 질주는 판 시계가 줄어든다. 클래식에는 기력·시계가 «없다». 고른 모드는 다시 켜도 남는다. (`tools/qa-modes.mjs`)
- [ ] D35. **처음 온 아이가 방법을 안다** — 첫 판에서 놀이 방법이 저절로 뜨고, 읽는 동안 판이 멈춰 있다(읽는 사이 기력이 줄면 읽자마자 죽는다). 닫으면 판이 다시 흐르고, 두 번째 판에는 뜨지 않으며, 「?」로 언제든 다시 볼 수 있다. 문구는 기기(터치/키보드)와 모드(하트/기력)에 맞게 바뀐다. 모바일·데스크톱 두 뷰포트에서 잰다. (`tools/qa-howto.mjs`)
  - 🔴 이 항목이 생긴 이유: 21개 게이트가 초록불인 채로 «발판을 누르라»는 말이 어디에도 없는 빌드가 배포됐다. 아무도 「설명이 있는가」를 재지 않았다.
- 해당 없음: 투사체 실체(4-0b-5) · 게임 시계 배속(4-0b-7, 배속 없음) · 난이도 캐릭터 라운드로빈(대전 아님, D10 이 대체).

## 1-9. 성능 예산
- **참조 기기(고정)**: 모바일 = Samsung Galaxy A34 5G(2023, Android 14, Chrome 최신 안정판, 1080×2340, DPR 2.625) — 보유 기기가 다르면 승인 단계에서 SKU 를 교체해 고정한다 / 데스크톱 = macOS Chrome 최신 안정판 1920×1080.
- **최악 장면**: 층 전환 카메라 이징 + 발판 3개 등장 + 스파클 12 + 불꽃 트레일 40 파티클 + 하늘 크로스페이드 + HUD 갱신 동시.
- **합격선(등급형, L-093)**: 데스크톱 P95 프레임 간격 ≤ 17.5ms(60Hz 등급), 모바일 ≤ 34ms(30Hz 등급) — 「최악 장면에서 쟀다」를 로그로 단언. 평균 fps 로 적지 않는다.
- **번들**: 초기 로드 ≤ 3MB(**gzip 전송량 = 첫 화면→첫 문제까지 HAR transferSize 합**), 번들 전체 ≤ 8MB. 🔴 **온디맨드 콘텐츠(`assets/say` 낱말 발음 943개)는 번들과 «따로» 잰다** — 문제당 4KB 를 하나씩 받는 자산이라 합산하면 «받지도 않는 3.8MB» 때문에 빨간불이 켜진다. 대신 온디맨드에도 상한(6MB)을 두고, 「첫 문제 전에 0건 전송」을 게이트가 직접 확인한다. 테마 에셋은 해금 시 지연 로딩(HAR 에 없어야 한다).
- **드로우콜 경고선**: 최악 장면 ≤ 60 — 최악 장면 스크립트에서 렌더러 드로우 카운터를 프레임마다 읽어 최대값을 로그 단언(D25).

## 1-10. 네트워크 규모
해당 없음(동시접속 없음). v2 온라인 랭킹 = 생성 닉네임 + `children-privacy-and-safety.md` + 서버 재검증, 별도 GDD 개정.

## 2. Phase 2 예고 (승인 후)
Phaser 3.90 + Vite, 로직 코어(`src/core/`: 문항 생성·판정·타이머·하트·SRS·경제)는 DOM/Phaser import 0 → headless 1:1 테스트. 스케일 확정은 Kenney CC0 임시 에셋으로(캐릭터 화면 높이 12%, 발판 폭 30%).

## 🚧 승인 게이트
이 문서의 §1-3(룰)·§1-4(수치)·§1-8(완료 기준)·§1-9(예산) 승인 전 **코드·에셋·HTML 0줄**. 사용자 결정 필요 항목: ① 게임 이름 확정(상표 검색) ② v1 과목 범위(구구단 + 영단어 3~6 ✓ / 덧셈·뺄셈 추가?) ③ 상업 모델(일회성 유료 앱 vs 학교 라이선스) ④ 참조 기기(§1-9 Galaxy A34) 보유 확인 또는 대체 SKU 확정.
